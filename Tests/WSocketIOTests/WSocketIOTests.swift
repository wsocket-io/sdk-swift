import XCTest
@testable import WSocketIO

final class WSocketIOTests: XCTestCase {
    func testClientInitialization() {
        // Placeholder test — verifies the library links correctly
        XCTAssertTrue(true, "WSocketIO module loaded successfully")
    }
}
